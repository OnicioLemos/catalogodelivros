{
  "name": "catalogolivros",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "jsonserver": "json-server --watch server.json --port 3000"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  },
  "dependencies": {
    "json-server": "^1.0.0-alpha.23"
  }
}
